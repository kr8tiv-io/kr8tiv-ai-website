{
  "_comment": "Project configuration for DESIGN-v2 hooks",
  "_description": "This file is created by project-hook-setup skill. Modify values as needed.",

  "project_type": "fastapi",
  "_project_type_options": ["fastapi", "django", "node", "python", "other"],

  "dev_server_port": 8000,
  "_description": "Local development server port",

  "health_check": "curl -sf http://localhost:8000/health",
  "_description": "Command to verify server is running",
  "_note": "For non-API projects, use 'echo skip' or remove this field",

  "test_command": "pytest",
  "_description": "Command to run tests",
  "_note": "Can be 'pytest', 'npm test', 'mvn test', etc.",

  "required_env": ["DATABASE_URL", "API_KEY"],
  "_description": "Environment variables that must be set",
  "_note": "Empty array [] if none required",

  "required_services": ["redis://localhost:6379"],
  "_description": "Services that must be running",
  "_format": "Use 'protocol://host:port' format, or just port number",
  "_note": "Empty array [] if none required"
}
