## Node.js-Specific Guidelines

| Purpose | Command |
|---------|---------|
| Install deps | `npm install` |
| Run dev | `npm run dev` |
| Build | `npm run build` |
| Lint | `npm run lint` |
| Test | `npm test` |

## Framework: {framework}

{framework_specific_commands}

## Entry Points

{node_entry_points}

## Common Patterns

- Use `import` for ES modules
- Environment variables in `.env`
- Use `concurrently` for running multiple scripts
